# Ch06 정답 — 카메라와 조명

**Q1.** X

활성화된 `Camera3D`가 없으면 화면이 회색이거나 아무것도 보이지 않는다.

---

**Q2.**
- A = `CameraPivot (Node3D)` — 회전 중심점
- B = `SpringArm3D` — 벽 감지 + 팔 길이 자동 조절
- C = `Camera3D` — 실제 카메라

---

**Q3.** SpringArm3D는 카메라를 플레이어 뒤에 일정 거리만큼 띄워두는 역할이다. 셀카봉처럼 카메라를 멀리 밀어내다가, 벽이 있으면 자동으로 팔을 짧게 줄여서 카메라가 벽 안으로 들어가지 않게 한다.

---

**Q4.** SpringArm3D는 경로에 있는 물체를 장애물로 인식하는데, 플레이어 자신의 `CapsuleShape3D`도 장애물로 인식할 수 있다. 그러면 점프/착지 시 카메라가 순간적으로 가까워지는 버그가 발생한다. `add_excluded_object(get_rid())`로 플레이어 자신을 제외 목록에 추가하면 수정된다.

---

**Q5.** 카메라가 약간 아래를 내려다보고 있을 때 방향 벡터에 Y 성분이 생긴다. Y를 0으로 만들지 않으면 "앞으로 가" 했을 때 공중으로 뜨거나 땅 속으로 파고드는 현상이 생긴다.

---

**Q6.** ③ `DirectionalLight3D`

---

**Q7.** 하늘 배경(Sky), 안개(Fog), 전체 분위기 빛(Ambient Light), Glow(빛 번짐), SSAO(환경광 차폐) 중 3가지

---

**Q8.** `OmniLight3D`는 전구처럼 모든 방향으로 균등하게 빛을 발산하고, `SpotLight3D`는 손전등처럼 특정 방향의 원뿔 형태로만 빛을 발산한다.

---

**Q9.** `rotation.y`가 `-0.3 라디안` 감소한다.

계산: 100픽셀 × 0.003 = 0.3, 마우스를 오른쪽으로 움직이면 오른쪽을 봐야 하므로 음수(-) 적용.
