# Ch04 정답 — Claude에게 잘 지시하는 법

**Q1.** B가 더 좋다.

A는 이동 방식, 노드 타입, 카메라 시점, 수치 등을 Claude가 임의로 결정해야 하므로 원하는 결과와 다를 가능성이 높다. B는 필요한 정보(엔진 버전, 노드 타입, 조작 방식, 수치, 파일 경로)가 구체적으로 포함되어 있어 한 번에 원하는 결과가 나온다.

---

**Q2.** ② Output 탭의 빨간 텍스트 전체

일부만 보내면 원인 파악이 어렵다.

---

**Q3.** ② NavigationMesh Bake 버튼 클릭

에디터 UI 상호작용은 Claude가 파일로 처리할 수 없다. 나머지는 모두 파일 직접 수정으로 처리 가능하다.

---

**Q4.** 에러가 났을 때 원인이 어디서 발생한 것인지 찾기 어렵다. 큰 기능을 한 번에 구현하다 문제가 생기면 어느 단계에서 발생했는지 격리할 수 없다.

---

**Q5.** B → C → D → (잘 됨: E) / (안 됨: A → C → D 반복)

1. B: Claude에게 기능 요청
2. C: Claude가 파일 수정/생성
3. D: Godot에서 F5/F6으로 실행
4. 잘 됨 → E(다음 기능), 안 됨 → A(에러 전달) → C → D 반복

---

**Q6.** "현재 동작"(지금 어떻게 되고 있는지)과 "원하는 동작"(어떻게 바뀌어야 하는지)을 명확히 구분해서 설명해야 한다.

---

**Q7.** X

Claude는 기본적으로 코드 블록을 제시하는 방식으로 답할 수도 있다. "직접 파일을 수정해줘"라고 명시해야 확실하다.
